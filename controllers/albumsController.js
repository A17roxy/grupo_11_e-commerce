const path = require('path');

const controller ={
    albums: (req,res) => {res.render('albums')}
}

module.exports = controller;
                                                                       